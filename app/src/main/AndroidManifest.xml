<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a21609143.mrfox"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherfox"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherfox"
        android:supportsRtl="true"
        android:theme="@style/MrFoxTheme">
        <activity android:name=".MySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MyLogin" />
        <activity android:name=".MyMain"></activity>
    </application>

</manifest>